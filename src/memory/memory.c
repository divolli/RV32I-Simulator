#include "memory/memory.h"

/* Just for reference
// Memory subsystem
typedef struct {
  uint8_t* data;
  size_t size;
  uint32_t base_address;
} memory_bank_t;

// Memory access sizes
typedef enum {
  MEM_SIZE_BYTE,
  MEM_SIZE_HALFWORD,
  MEM_SIZE_WORD
} memory_size_t
*/
